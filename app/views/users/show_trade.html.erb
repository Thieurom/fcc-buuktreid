<% provide(:title, @user.username + " | Buuktreid") %>
<% content_for :sidebar do %>
  <%= render partial: 'menu', locals: { active_menu_link: 'Trading' } %>
<% end %>
<% content_for :detail do %>
  <ol class="list">
    <%= render @books %>
  </ol>
<% end %>
